# create user table

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    firstname VARCHAR(50),
    lastname VARCHAR(50),
    role ENUM('user','admin','super_admin') NOT NULL DEFAULT 'user',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    is_active TINYINT(1) NOT NULL DEFAULT 1,
);

#create users entries

INSERT INTO users 
(username, email, password, role, firstname, lastname, created_at)
VALUES (
    'superadmin',
    'superadmin@yourdomain.com',
    '$2y$10$N9qo8uLOickgx2ZMRZoMy.MrE1L6klg4hANWsvCa3.7wl8s1PlXeO',
    'super_admin',
    'System',
    'Admin',
    NOW()
);

# Create locations/emplacements table

CREATE TABLE locations (
    id INT AUTO_INCREMENT PRIMARY KEY,
    type ENUM('tente', 'caravane', 'chalet') NOT NULL,
    price_per_night DECIMAL(10,2) NOT NULL,
    capacity INT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

# Create locations entries

INSERT INTO locations (type, price_per_night, capacity) 
VALUES ('tente', 50.00, 4);